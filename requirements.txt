# Core engine (atpp.core, atpp.benchmark, atpp.protocol, atpp.compositional)
numpy>=1.22

# Figures and experiment scripts
matplotlib>=3.5

# Comparison baselines (atpp.baselines: one-class SVM, PU, isolation forest, MCD)
scikit-learn>=1.1

# Semi-synthetic experiment only (experiments/semisynth.py)
pandas>=1.4

# Interactive notebook only
ipywidgets>=7.7

# Test suite only
pytest>=7.0
